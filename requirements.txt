Django
Pillow
python-decouple
djangorestframework
selenium
mysqlclient
textblob
geopy

# If "pip install plotly.express" doesn't work,
# try "pip install plotly.express --no-cache-dir"
plotly.express 